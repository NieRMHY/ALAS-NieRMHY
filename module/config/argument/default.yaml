# --------------------
# Define default values
# --------------------

# ==================== Alas ====================

# ==================== Farm ====================

# ==================== Event ====================

EventA:
  EventDaily:
    StageFilter: |-
      A1 > A2 > A3
EventB:
  EventDaily:
    StageFilter: |-
      B1 > B2 > B3
EventC:
  EventDaily:
    StageFilter: |-
      C1 > C2 > C3
EventD:
  EventDaily:
    StageFilter: |-
      D1 > D2 > D3

# Add by MHY, 共斗刷好感：心情控制默认防止绿脸(>40)，与好感计数门槛对齐
RaidAffection:
  Emotion:
    Fleet1Control: prevent_green_face
    Fleet2Control: prevent_green_face

# ==================== Reward ====================

# ==================== DailyReward ====================

# ==================== DailyMission ====================

# ==================== Opsi ====================
ThreeOilLowCost:
  Scheduler:
    Enable: false
OpsiCrossMonth:
  Scheduler:
    Sensitive: true
OpsiObscure:
  Scheduler:
    Sensitive: true
OpsiAbyssal:
  Scheduler:
    Sensitive: true
